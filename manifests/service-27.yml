apiVersion: v1
kind: Service
metadata:
    name: "diaggarw-476f"
    labels:
        app: "diaggarw-476f"
spec:
    type: ClusterIP
    ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
    selector:
        app: "diaggarw-476f"